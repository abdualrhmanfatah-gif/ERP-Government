# AGENTS.md — ERP-Government

Detail offloaded to cavemem: recurring context block 656faaa70d4de653.
Recall with `caveman mem recall "AGENTS.md-ERP-Gov-current"` for the compact form.
Byte-exact original via `caveman mem recover ccr_288fbc030634ab43091d7297220afa95`.

## Frontend Engineering Rules

- Inspect existing implementations before proposing changes.
- Prefer existing shared UI components.
- Preserve the current design identity.
- Maintain RTL compatibility.
- Preserve existing API contracts.
- Never fabricate test results.
- Verify changes using relevant project checks.
- Report pre-existing failures separately.
- Keep unrelated files unchanged.
- Require approval before large refactors.
